/**
 * Features Module
 *
 * This module exports all feature modules.
 * Each feature is a self-contained unit with its own components, hooks, services, and types.
 */

// Feature exports will be added as features are migrated
export {};
